output$Project <- renderText({ "Dynamic data visualization with R project" })

output$Authors <- renderText({ "Authors"})

output$Authors1 <- renderText({ "Otis Cooper"})

output$Authors2 <- renderText({ "Stein Dijkstra"})